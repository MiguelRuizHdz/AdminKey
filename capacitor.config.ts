import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.miguelruizhdz.adminkey',
  appName: 'Admin Key',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
